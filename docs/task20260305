P0-1：生成结果在当前标签页持续保留（直到网页关闭）

类型：Bugfix + 状态一致性
标题：生成结果追加保留（Tab 生命周期）
描述：每次生成的新图必须追加到结果区，不覆盖历史结果；在当前标签页内（刷新/路由切换）持续保留，关闭标签页后自然失效。
模块归属：Studio Genesis / Ecom Studio 结果区与会话恢复层
用户操作路径：生成第1批 → 生成第2批 → 刷新/切页返回 → 结果仍在 → 关闭标签页后再打开页面 → 不自动恢复旧结果
API 调用影响点：生成链路保持不变（analyze、prompts、generate、job轮询）；新增/强化前端会话写入与恢复时机
验收标准（Given/When/Then）：
Given 已生成多批，When 再次生成，Then 新结果追加且旧结果不丢失
Given 已有结果，When 刷新或切路由返回，Then 结果完整恢复
Given 关闭当前标签页，When 新开标签进入，Then 不恢复该标签页历史结果
回归检查项：批次标签/时间戳显示正确；预览/下载/再编辑不受影响；多产品分组索引不串位
风险：会话恢复与实时状态竞争导致重复插入或错序
回滚策略：关闭“追加保留”开关，回退旧结果策略（仅当前批次）
依赖：会话状态结构稳定（结果ID、批次ID、时间戳）
P0-2：单图输入下正背面双平铺强制产出

类型：Feature + 规则补齐
标题：单图 + 勾选正面背面 => 必产两张平铺图
描述：当仅上传1张产品图且勾选“正面+背面”时，必须同时生成1张正面平铺和1张背面平铺；不可合并为单张。
模块归属：Clothing Studio → Basic Photo Set（白底精修类型）
用户操作路径：上传1张图 → 勾选正面/背面 → Analyze → Generate → 结果区看到正面平铺+背面平铺
API 调用影响点：
Analyze：生成两条 image plan（front_flatlay/back_flatlay）
Prompt：两条 plan 各自产生独立提示词
Generate：按两条 plan 创建两条 job
验收标准（Given/When/Then）：
Given 1张图且勾选正面+背面，When 生成完成，Then 返回2张且方向标签正确
Given 仅勾选正面（或背面），When 生成，Then 仅产出对应1张
Given 一方向失败，When 结束，Then 成功图保留且失败项可单独重试
回归检查项：多图输入逻辑不变；3D/人台/细节特写不受影响；会话恢复后方向标签与图片仍一一对应
风险：plan与结果映射错位导致正背面标记反转
回滚策略：开关关闭该规则，退回旧生成逻辑
依赖：Basic Photo Set 已有方向选择字段与 plan 映射能力